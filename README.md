
Misc

```
./gradlew runPercolation          # Run Percolation
./gradlew run                     # Still runs default App class
```

Run this to build
```
./gradlew build
```

Run this to build without checkstyle
```
./gradlew build -x checkstyleMain -x checkstyleTest
```